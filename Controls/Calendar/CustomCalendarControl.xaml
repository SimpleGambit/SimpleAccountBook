<UserControl x:Class="SimpleAccountBook.Controls.Calendar.CustomCalendarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<!-- 날짜 칸 스타일 -->
		<Style x:Key="DayCellStyle" TargetType="Border">
			<Setter Property="BorderBrush" Value="#E0E0E0" />
			<Setter Property="BorderThickness" Value="0.5" />
			<Setter Property="Background" Value="White" />
			<Setter Property="MinHeight" Value="60" />
		</Style>

		<!-- 요일 헤더 스타일 -->
		<Style x:Key="DayHeaderStyle" TargetType="TextBlock">
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="HorizontalAlignment" Value="Center" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="Padding" Value="5" />
		</Style>

		<!-- 주간 합계 스타일 -->
		<Style x:Key="WeeklySumStyle" TargetType="Border">
			<Setter Property="BorderBrush" Value="#D0D0D0" />
			<Setter Property="BorderThickness" Value="0.5" />
			<Setter Property="Background" Value="#F8F8F8" />
			<Setter Property="MinHeight" Value="60" />
		</Style>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<!-- 년월 네비게이션 -->
			<RowDefinition Height="Auto" />
			<!-- 요일 헤더 -->
			<RowDefinition Height="*" />
			<!-- 달력 본체 -->
		</Grid.RowDefinitions>

		<!-- 년월 네비게이션 -->
		<Border Grid.Row="0" Background="#F5F5F5" Padding="5">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<Button x:Name="PrevMonthButton"
                        Grid.Column="0"
                        Content="◀"
                        Width="30"
                        Height="30"
                        Click="PrevMonthButton_Click" />

				<TextBlock x:Name="CurrentMonthText"
                           Grid.Column="1"
                           Text="2025년 9월"
                           FontSize="16"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />

				<Button x:Name="NextMonthButton"
                        Grid.Column="2"
                        Content="▶"
                        Width="30"
                        Height="30"
                        Click="NextMonthButton_Click" />
			</Grid>
		</Border>

		<!-- 요일 헤더 -->
		<Grid Grid.Row="1" Background="#F0F0F0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<!-- 일 -->
				<ColumnDefinition Width="*" />
				<!-- 월 -->
				<ColumnDefinition Width="*" />
				<!-- 화 -->
				<ColumnDefinition Width="*" />
				<!-- 수 -->
				<ColumnDefinition Width="*" />
				<!-- 목 -->
				<ColumnDefinition Width="*" />
				<!-- 금 -->
				<ColumnDefinition Width="*" />
				<!-- 토 -->
				<ColumnDefinition Width="100" />
				<!-- 주간합계 -->
			</Grid.ColumnDefinitions>

			<Border Grid.Column="0" BorderBrush="#D0D0D0" BorderThickness="0.5">
				<TextBlock Text="일" Style="{StaticResource DayHeaderStyle}" Foreground="Red" />
			</Border>
			<Border Grid.Column="1" BorderBrush="#D0D0D0" BorderThickness="0.5">
				<TextBlock Text="월" Style="{StaticResource DayHeaderStyle}" />
			</Border>
			<Border Grid.Column="2" BorderBrush="#D0D0D0" BorderThickness="0.5">
				<TextBlock Text="화" Style="{StaticResource DayHeaderStyle}" />
			</Border>
			<Border Grid.Column="3" BorderBrush="#D0D0D0" BorderThickness="0.5">
				<TextBlock Text="수" Style="{StaticResource DayHeaderStyle}" />
			</Border>
			<Border Grid.Column="4" BorderBrush="#D0D0D0" BorderThickness="0.5">
				<TextBlock Text="목" Style="{StaticResource DayHeaderStyle}" />
			</Border>
			<Border Grid.Column="5" BorderBrush="#D0D0D0" BorderThickness="0.5">
				<TextBlock Text="금" Style="{StaticResource DayHeaderStyle}" />
			</Border>
			<Border Grid.Column="6" BorderBrush="#D0D0D0" BorderThickness="0.5">
				<TextBlock Text="토" Style="{StaticResource DayHeaderStyle}" Foreground="Blue" />
			</Border>
			<Border Grid.Column="7" BorderBrush="#D0D0D0" BorderThickness="0.5" Background="#F0F0F0">
				<TextBlock Text="주간합계" Style="{StaticResource DayHeaderStyle}" />
			</Border>
		</Grid>

		<!-- 달력 본체 (6주 x 8칸) -->
		<Grid x:Name="CalendarGrid" Grid.Row="2">
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="*" />
				<RowDefinition Height="*" />
				<RowDefinition Height="*" />
				<RowDefinition Height="*" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="100" />
			</Grid.ColumnDefinitions>

			<!-- 동적으로 날짜 칸과 주간합계 칸이 추가됨 -->
		</Grid>
	</Grid>
</UserControl>