<Window x:Class="SimpleAccountBook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:SimpleAccountBook.Converters;assembly=SimpleAccountBook"
        xmlns:calendar="clr-namespace:SimpleAccountBook.Controls.Calendar"
        mc:Ignorable="d"
        Title="간단한 가계부"
        Height="720"
		Width="1280"
        AllowDrop="True"
        DragEnter="Window_DragEnter"
        Drop="Window_Drop">
	<Window.Resources>
		<converters:DaySummaryConverter x:Key="DaySummaryConverter" />
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
		<converters:TransactionTypeToBrushConverter x:Key="TransactionTypeToBrushConverter" />
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
        <converters:AmountDifferenceValueConverter x:Key="AmountDifferenceValueConverter" />
        <converters:AmountDifferenceVisibilityConverter x:Key="AmountDifferenceVisibilityConverter" />

        <!-- 거래 항목 스타일 -->
		<Style x:Key="TransactionExpanderStyle" TargetType="Expander">
			<Setter Property="BorderBrush" Value="#E0E0E0"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Margin" Value="0,2"/>
			<Setter Property="Background" Value="White"/>
		</Style>
		
		<!-- 상세 내역 항목 스타일 -->
		<Style x:Key="DetailBorderStyle" TargetType="Border">
			<Setter Property="Background" Value="#FAFAFA"/>
			<Setter Property="BorderBrush" Value="#E0E0E0"/>
			<Setter Property="BorderThickness" Value="0,1,0,0"/>
			<Setter Property="Margin" Value="5,2,2,2"/>
			<Setter Property="Padding" Value="5"/>
		</Style>
		
		<!-- 레이블 스타일 -->
		<Style x:Key="FieldLabelStyle" TargetType="TextBlock">
			<Setter Property="FontSize" Value="10"/>
			<Setter Property="Foreground" Value="#666666"/>
			<Setter Property="Margin" Value="0,0,0,2"/>
		</Style>
	</Window.Resources>
	
	<Grid Margin="16">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="3*" />
			<ColumnDefinition Width="2*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Border Grid.ColumnSpan="2"
                Padding="12"
                Margin="0 0 0 12"
                Background="#f5f5f5"
                CornerRadius="6">
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="엑셀 파일(.xlsx)을 달력 위에 드래그하거나, 버튼으로 선택하세요."/>
                    <Button Content="파일 선택" Click="SelectFileButton_Click" Padding="16 4" Margin="12 0 0 0"/>
                    <Button Content="환경설정" Click="OpenSettings_Click" Padding="16 4" Margin="12 0 0 0"/>
                    <Button Content="상태 저장" Click="SaveStateButton_Click" Padding="16 4" Margin="12 0 0 0"/>
                    <Button Content="상태 불러오기" Click="LoadStateButton_Click" Padding="16 4" Margin="8 0 0 0"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12 0 0 0">
                        <TextBlock Text="상태:" FontWeight="Bold" />
                        <TextBlock Text="{Binding StatusMessage}" Margin="8 0 0 0" />
                    </StackPanel>
                    <ProgressBar Width="120"
                                     Margin="12 0 0 0"
                                     Height="16"
                                     IsIndeterminate="{Binding IsBusy}"
                                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal"
                                            Margin="0 8 0 0"
                                            Visibility="{Binding LoadedFiles.Count, Converter={StaticResource CountToVisibilityConverter}}"
                                            VerticalAlignment="Center">
                    <TextBlock Text="불러온 파일:" FontWeight="Bold" VerticalAlignment="Center" />
                    <ItemsControl ItemsSource="{Binding LoadedFiles}" Margin="8 0 0 0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#ffffff"
                                                                        BorderBrush="#d0d0d0"
                                                                        BorderThickness="1"
                                                                        CornerRadius="4"
                                                                        Margin="0 0 8 4"
                                                                        Padding="6 4"
                                                                        ToolTip="{Binding FilePath}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding TransactionCount, StringFormat=' ({0}건)'}" Margin="4 0 8 0" Foreground="#666666" />
                                        <Button Content="✕"
                                                                                        Width="20"
                                                                                        Height="20"
                                                                                        Padding="0"
                                                                                        Margin="0"
                                                                                        Tag="{Binding FilePath}"
                                                                                        Click="RemoveLoadedFile_Click"
                                                                                        ToolTip="이 파일 제거"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Margin="0 0 12 0"
                Padding="12"
                BorderBrush="#d0d0d0"
                BorderThickness="1"
                CornerRadius="6">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>
				<TextBlock Text="월간 달력"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0 0 0 8" />
				
				<!-- 커스텀 달력 컨트롤 -->
				<calendar:CustomCalendarControl x:Name="AccountCalendar"
												 Grid.Row="1"
												 SelectedDate="{Binding SelectedDate, Mode=TwoWay}"
												 DisplayDate="{Binding CurrentMonth, Mode=TwoWay}" />
			</Grid>
		</Border>

		<Border Grid.Column="1"
                Grid.Row="1"
                Padding="12"
                BorderBrush="#d0d0d0"
                BorderThickness="1"
                CornerRadius="6">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>
				<StackPanel Orientation="Vertical">
					<TextBlock Text="월별 합계" FontSize="18" FontWeight="Bold" />
                    <TextBlock Text="{Binding CurrentMonth, StringFormat='{}{0:yyyy년 MM월}'}" FontWeight="SemiBold" Margin="0 4 0 0" />
                </StackPanel>
				<UniformGrid Grid.Row="1" Columns="2" Margin="0 8 0 0" Rows="2" >
					<StackPanel>
						<TextBlock Text="총 수입" FontWeight="Bold" />
                        <TextBlock Text="{Binding MonthlyIncome, StringFormat='{}{0:#,0}원'}" Foreground="DarkGreen" FontSize="16" />
                    </StackPanel>
                    <StackPanel>
						<TextBlock Text="총 지출" FontWeight="Bold" />
                        <TextBlock Text="{Binding MonthlyExpense, StringFormat='{}{0:#,0}원'}" Foreground="Firebrick" FontSize="16" />
					    </StackPanel>
					<StackPanel>
						<TextBlock Text="순이익" FontWeight="Bold" />
                        <TextBlock Text="{Binding MonthlyNet, StringFormat='{}{0:#,0}원'}" FontSize="16" />
                        </StackPanel>
					<StackPanel>
						<TextBlock Text="현재 자산" FontWeight="Bold" />
                        <TextBlock Text="{Binding AssetBalance, StringFormat='{}{0:#,0}원'}" FontSize="16" />
                        </StackPanel>
				</UniformGrid>
				<StackPanel Grid.Row="2" Margin="0 8 0 8">
					<TextBlock Text="선택한 날짜의 거래" FontSize="16" FontWeight="Bold" />
                    <TextBlock Text="{Binding SelectedDate, StringFormat='{}{0:yyyy-MM-dd}'}" />
                </StackPanel>
				
				<!-- 거래 목록 (상세내역 기능 포함) -->
				<ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
					<Grid>
						<TextBlock Text="선택한 날짜의 거래가 없습니다."
								   Foreground="Gray"
								   HorizontalAlignment="Center"
								   VerticalAlignment="Center">
							<TextBlock.Style>
								<Style TargetType="TextBlock">
									<Setter Property="Visibility" Value="Collapsed" />
									<Style.Triggers>
										<DataTrigger Binding="{Binding SelectedDateTransactionsWithMemo.Count}" Value="0">
											<Setter Property="Visibility" Value="Visible" />
										</DataTrigger>
									</Style.Triggers>
								</Style>
							</TextBlock.Style>
						</TextBlock>
						
						<ItemsControl ItemsSource="{Binding SelectedDateTransactionsWithMemo}">
							<ItemsControl.ItemTemplate>
								<DataTemplate>
									<Expander Style="{StaticResource TransactionExpanderStyle}"
											  IsExpanded="{Binding IsExpanded}">
										<Expander.Header>
											<!-- 거래 요약 정보 -->
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="50"/>
													<ColumnDefinition Width="40"/>
													<ColumnDefinition Width="90"/>
													<ColumnDefinition Width="100"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												
												<TextBlock Grid.Column="0" 
														   Text="{Binding TransactionTime, StringFormat='{}{0:HH:mm}'}"
														   VerticalAlignment="Center"/>
												
												<TextBlock Grid.Column="1" 
														   Text="{Binding TransactionType}"
														   Foreground="{Binding TransactionType, Converter={StaticResource TransactionTypeToBrushConverter}}"
														   VerticalAlignment="Center"
														   HorizontalAlignment="Center"/>
												
												<TextBlock Grid.Column="2" 
														   Text="{Binding Amount, StringFormat='{}{0:#,0}'}"
														   Foreground="{Binding TransactionType, Converter={StaticResource TransactionTypeToBrushConverter}}"
														   HorizontalAlignment="Right"
														   VerticalAlignment="Center"
														   Margin="0,0,5,0"/>
												
												<TextBlock Grid.Column="3" 
														   Text="{Binding Category}"
														   VerticalAlignment="Center"
														   TextTrimming="CharacterEllipsis"
														   Margin="5,0"/>
												
												<TextBlock Grid.Column="4" 
														   Text="{Binding Description}"
														   VerticalAlignment="Center"
														   TextTrimming="CharacterEllipsis"
														   Margin="5,0"/>
											</Grid>
										</Expander.Header>
										
										<!-- 상세 내역 패널 -->
										<Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="10">
											<StackPanel>
												<!-- 상세 내역 헤더 -->
												<Grid Margin="0,0,0,5">
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="*"/>
														<ColumnDefinition Width="Auto"/>
													</Grid.ColumnDefinitions>
													<TextBlock Text="상세 내역" FontWeight="Bold" FontSize="14"/>
													<Button Grid.Column="1" 
															Content="항목 추가" 
															Click="AddMemoDetail_Click"
															Tag="{Binding}"
															Padding="8,3"
															FontSize="12"/>
												</Grid>
												
												<!-- 필드 레이블 헤더 (첫 번째 항목이 있을 때만 표시) -->
												<Grid Margin="5,5,2,0" Visibility="{Binding Memo.Details.Count, Converter={StaticResource CountToVisibilityConverter}}">
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="2*"/>
														<ColumnDefinition Width="100"/>
														<ColumnDefinition Width="3*"/>
														<ColumnDefinition Width="30"/>
													</Grid.ColumnDefinitions>
													
													<TextBlock Grid.Column="0" Text="항목명" Style="{StaticResource FieldLabelStyle}" Margin="5,0,0,2"/>
													<TextBlock Grid.Column="1" Text="금액" Style="{StaticResource FieldLabelStyle}" HorizontalAlignment="Right" Margin="0,0,5,2"/>
													<TextBlock Grid.Column="2" Text="메모" Style="{StaticResource FieldLabelStyle}" Margin="5,0,0,2"/>
												</Grid>
												
												<!-- 상세 내역 리스트 -->
												<ItemsControl ItemsSource="{Binding Memo.Details}">
													<ItemsControl.ItemTemplate>
														<DataTemplate>
															<Border Style="{StaticResource DetailBorderStyle}">
																<Grid>
																	<Grid.ColumnDefinitions>
																		<ColumnDefinition Width="2*"/>
																		<ColumnDefinition Width="100"/>
																		<ColumnDefinition Width="3*"/>
																		<ColumnDefinition Width="30"/>
																	</Grid.ColumnDefinitions>
																	
																	<TextBox Grid.Column="0" 
																			 Text="{Binding ItemName, UpdateSourceTrigger=PropertyChanged}"
																			 VerticalAlignment="Center"
																			 BorderThickness="1"
																			 BorderBrush="#DDDDDD"
																			 Padding="3"
																			 Margin="0,0,5,0"/>
																	
																	<TextBox Grid.Column="1" 
																			 Text="{Binding Amount, StringFormat='{}{0:#,0}', UpdateSourceTrigger=PropertyChanged}"
																			 HorizontalAlignment="Stretch"
																			 TextAlignment="Right"
																			 VerticalAlignment="Center"
																			 BorderThickness="1"
																			 BorderBrush="#DDDDDD"
																			 Padding="3"
																			 Margin="0,0,5,0"/>
																	
																	<TextBox Grid.Column="2" 
																			 Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}"
																			 VerticalAlignment="Center"
																			 BorderThickness="1"
																			 BorderBrush="#DDDDDD"
																			 Padding="3"
																			 Margin="0,0,5,0"/>
																	
																	<Button Grid.Column="3" 
																			Content="×" 
																			Click="RemoveMemoDetail_Click"
																			Tag="{Binding}"
																			Background="#FFE0E0"
																			BorderBrush="#FFCCCC"
																			BorderThickness="1"
																			Foreground="Red"
																			FontSize="16"
																			FontWeight="Bold"
																			Width="25"
																			Height="25"
																			Cursor="Hand"/>
																</Grid>
															</Border>
														</DataTemplate>
													</ItemsControl.ItemTemplate>
												</ItemsControl>
												
												<!-- 상세 항목이 없을 때 안내 메시지 -->
												<Border Padding="10" Margin="5,5,2,0" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1">
													<TextBlock Text="상세 내역을 추가하려면 '항목 추가' 버튼을 클릭하세요."
															   Foreground="#999999"
															   FontStyle="Italic"
															   HorizontalAlignment="Center">
														<TextBlock.Style>
															<Style TargetType="TextBlock">
																<Setter Property="Visibility" Value="Collapsed"/>
																<Style.Triggers>
																	<DataTrigger Binding="{Binding Memo.Details.Count}" Value="0">
																		<Setter Property="Visibility" Value="Visible"/>
																	</DataTrigger>
																</Style.Triggers>
															</Style>
														</TextBlock.Style>
													</TextBlock>
												</Border>
												
												<!-- 상세 항목 합계 -->
                                                <Border Background="#E8F5E9"
                                                        BorderBrush="#C8E6C9"
                                                        BorderThickness="1"
                                                        Padding="8"
                                                        Margin="0,8,0,0">
                                                    <Border.Visibility>
                                                        <MultiBinding Converter="{StaticResource AmountDifferenceVisibilityConverter}">
                                                            <Binding Path="Amount"/>
                                                            <Binding Path="Memo.TotalDetailAmount"/>
                                                            <Binding Path="Memo.Details.Count"/>
                                                        </MultiBinding>
                                                    </Border.Visibility>
                                                    <Grid>
                                                        <TextBlock Text="거래 금액과 상세 내역 합계의 차이:" FontWeight="Bold" HorizontalAlignment="Left"/>
                                                        <TextBlock FontWeight="Bold"
                                                                   FontSize="14"
                                                                   HorizontalAlignment="Right"
                                                                   Foreground="{Binding TransactionType, Converter={StaticResource TransactionTypeToBrushConverter}}">
                                                            <TextBlock.Text>
                                                                <MultiBinding Converter="{StaticResource AmountDifferenceValueConverter}" StringFormat="{}{0:#,0}원">
                                                                    <Binding Path="Amount"/>
                                                                    <Binding Path="Memo.TotalDetailAmount"/>
                                                                </MultiBinding>
                                                            </TextBlock.Text>
                                                        </TextBlock>
                                                    </Grid>
                                                </Border>
                                            </StackPanel>
										</Border>
									</Expander>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</Grid>
				</ScrollViewer>
			</Grid>
		</Border>

		<TextBlock Grid.Row="2"
                   Grid.ColumnSpan="2"
                   Margin="0 12 0 0"
                   Text="엑셀의 컬럼: 거래일시, 구분, 거래금액, 거래구분, 내용"
                   Foreground="DimGray" />
	</Grid>
</Window>